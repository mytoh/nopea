
(install
  (
    (run-process `(gauche-install -T ,(build-path ( *lehti-dist-directory* ) "nopea")
                                  "bin/nopea")
                 :wait #t)
   ;; library sources
   (copy-directory* (build-path ( *lehti-cache-directory* ) "nopea/lib")
                    (build-path ( *lehti-dist-directory* ) "nopea/lib"))
))
