
(install
  (
    (run-process `(gauche-install -T ,(build-path ( *lehti-dist-directory* ) "nopea")
                                  "bin/nopea")
                 :wait #t)
   ;; library sources
   (copy-directory* (build-path ( *lehti-cache-directory* ) "nopea/src")
                    (build-path ( *lehti-dist-directory* ) "nopea/src"))
))
